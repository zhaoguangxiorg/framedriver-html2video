# ========== 输出目录 ==========
# 项目产物输出根目录（相对路径基于项目根目录）
# 默认值：./output，即「项目根目录/output」（如 D:/.../ai_coding_trae_view/output）
# 不配置时自动使用默认值，无需填写
OUTPUT_BASE_DIR=./output

# ========== 加密密钥 ==========
# model_configs.api_key 的 Fernet 加密密钥（shared/crypto.py 读取）
# 若缺失，系统会自动生成并追加写入 .env，无需手动填写
# 注意：一旦生成请勿随意变更，否则已加密的 API Key 将无法解密
# MODEL_KEY_ENCRYPTION_KEY=
